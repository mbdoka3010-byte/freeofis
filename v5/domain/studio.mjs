import{createId,ownership,timestamp}from'./foundation.mjs';const text=(v,n)=>{const x=String(v||'').trim();if(!x)throw TypeError(`${n} is required.`);return x},base=(i,c)=>{const at=i.createdAt||timestamp(c.clock);return{id:i.id||createId(c.idOptions),...ownership(i),createdByUserId:text(i.createdByUserId,'creator'),createdAt:at,updatedAt:i.updatedAt||at,status:i.status||'idea'}};export const STORY_STATUSES=Object.freeze(['idea','researching','drafting','review','ready','published','complete','archived']);export function createStory(i={},c={}){if(!STORY_STATUSES.includes(i.status||'idea'))throw TypeError('Story status is invalid.');return{...base(i,c),projectId:text(i.projectId,'Story Project'),title:text(i.title,'Story title'),storyType:i.storyType||'general',brief:String(i.brief||''),deadlineAt:i.deadlineAt||null,publishedAt:i.publishedAt||null,externalReference:i.externalReference||null}}export function createDraft(i={},c={}){return{...base({...i,status:i.status||'drafting'},c),documentId:text(i.documentId,'Draft Document'),storyId:text(i.storyId,'Story'),title:text(i.title,'Draft title'),draftType:i.draftType||'general',body:String(i.body||''),sourceIds:[...new Set(i.sourceIds||[])],mediaFileIds:[...new Set(i.mediaFileIds||[])]}}

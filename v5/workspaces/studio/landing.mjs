const e=x=>String(x??'').replace(/[&<>]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;'}[c])),list=(a,m,f)=>a.length?a.map(f).join(''):`<p class="empty">${m}</p>`;export async function renderStudioLanding(host,{platform,token,workspace,context}){const s={ownerType:workspace.ownerType,ownerId:workspace.ownerId,organisationId:workspace.organisationId,unitId:workspace.unitId,workspaceId:workspace.id},h=await platform.studio.home(token,s);host.innerHTML=`<a href="#home">← Free Ofis Home</a><p class="eyebrow">${e(context.label)}</p><h1>My Studio</h1><p>Create and publish.</p><nav class="office-tabs" aria-label="My Studio sections">${['Home','Stories / Projects','Drafts','Assignments','Sources','Calendar','Media'].map(x=>`<button>${x}</button>`).join('')}</nav><div class="office-grid"><section class="section"><h2>Active stories</h2>${list(h.stories,'No active stories yet.',x=>`<p><strong>${e(x.title)}</strong><br>${e(x.status)}</p>`)}</section><section class="section"><h2>Assignments</h2>${list(h.assignments,'No assignments pending.',x=>`<p><strong>${e(x.title)}</strong></p>`)}</section><section class="section"><h2>Upcoming</h2>${list(h.upcoming,'No interviews or events coming up.',x=>`<p><strong>${e(x.title)}</strong><br>${new Date(x.startAt).toLocaleString()}</p>`)}</section><section class="section"><h2>Drafts</h2>${list(h.drafts,'No drafts in progress.',x=>`<p><strong>${e(x.title)}</strong></p>`)}</section></div>`}

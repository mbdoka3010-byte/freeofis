const e=x=>String(x??'').replace(/[&<>]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;'}[c])),list=(a,m,f)=>a.length?a.map(f).join(''):`<p class="empty">${m}</p>`;export async function renderSpaceLanding(host,{platform,token,workspace}){const s={ownerType:'personal',ownerId:workspace.ownerId,organisationId:null,unitId:null,workspaceId:workspace.id},h=await platform.space.home(token,s);host.innerHTML=`<a href="#home">← Free Ofis Home</a><p class="eyebrow">Private to you</p><h1>My Space</h1><p>Organise your personal life.</p><nav class="office-tabs" aria-label="My Space sections">${['Home','Tasks','Notes','Files','Reminders','Goals','Records'].map(x=>`<button>${x}</button>`).join('')}</nav><div class="office-grid"><section class="section"><h2>Today</h2>${list(h.dueToday,'Nothing is due today.',x=>`<p><strong>${e(x.title)}</strong></p>`)}</section><section class="section"><h2>Reminders</h2>${list(h.reminders,'No upcoming reminders.',x=>`<p><strong>${e(x.title)}</strong><br>${new Date(x.startAt).toLocaleString()}</p>`)}</section><section class="section"><h2>Goals</h2>${list(h.goals,'No active goals yet. What would you like to achieve?',x=>`<p><strong>${e(x.title)}</strong></p>`)}</section><section class="section"><h2>Recent notes and files</h2>${list([...h.recentNotes,...h.recentFiles],'Your personal notes and files will appear here.',x=>`<p><strong>${e(x.title)}</strong></p>`)}</section></div>`}

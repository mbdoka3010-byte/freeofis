const escape=value=>String(value??'').replace(/[&<>"']/g,char=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[char]));
export function renderWorkspaceLanding(host,{definition,workspace,context}){host.innerHTML=`<section class="workspace-landing" aria-labelledby="workspace-title"><a class="back-link" href="#home">← Free Ofis Home</a><p class="eyebrow">${escape(context.label)}${context.detail?` · ${escape(context.detail)}`:''}</p><h1 id="workspace-title">${escape(workspace.name||definition.name)}</h1><p>This workspace is ready. Its focused tools will arrive in a later phase.</p></section>`}

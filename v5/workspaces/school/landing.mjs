const esc=x=>String(x??'').replace(/[&<>]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;'}[c])),empty=(a,m,f)=>a.length?a.map(f).join(''):`<p class="empty">${m}</p>`;export async function renderSchoolLanding(host,{platform,token,workspace,context}){const scope={ownerType:workspace.ownerType,ownerId:workspace.ownerId,organisationId:workspace.organisationId,unitId:workspace.unitId,workspaceId:workspace.id},home=await platform.school.home(token,scope),fees=home.experience.experienceRole==='student'?'Fees':'Results';host.innerHTML=`<a class="back-link" href="#home">← Free Ofis Home</a><p class="eyebrow">${esc(context.label)} · ${esc(home.experience.experienceRole)}</p><h1>My School</h1><p>Learn and Teach.</p><nav class="office-tabs" aria-label="My School sections">${['Today','Courses','Timetable','Assignments','Materials','Notes','Exams',fees].map(x=>`<button>${x}</button>`).join('')}</nav><div class="office-grid"><section class="section"><h2>Upcoming classes</h2>${empty(home.classes,'No upcoming classes.',x=>`<p><strong>${esc(x.title)}</strong><br>${new Date(x.startAt).toLocaleString()}</p>`)}</section><section class="section"><h2>Assignments</h2>${empty(home.assignments,'No assignments due.',x=>`<p><strong>${esc(x.title)}</strong><br>Due ${new Date(x.dueAt).toLocaleString()}</p>`)}</section><section class="section"><h2>Courses</h2>${empty(home.courses,'No courses yet.',x=>`<p><strong>${esc(x.title)}</strong> ${esc(x.code||'')}</p>`)}</section><section class="section"><h2>Exams</h2>${empty(home.exams,'No upcoming exams.',x=>`<p><strong>${esc(x.title)}</strong></p>`)}</section></div>`}

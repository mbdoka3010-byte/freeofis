const sections=document.querySelectorAll('.section');const controls=document.querySelectorAll('[data-section]');const title=document.getElementById('title');const titles={home:'Your workspace',records:'Records',inventory:'Inventory',media:'Media',documents:'Documents',tools:'AI Tools',settings:'Settings',help:'Help'};function show(name){sections.forEach(s=>s.classList.toggle('show',s.id===name));document.querySelectorAll('.nav').forEach(n=>n.classList.toggle('active',n.dataset.section===name));title.textContent=titles[name]||'Your workspace')}controls.forEach(c=>c.addEventListener('click',()=>show(c.dataset.section)));
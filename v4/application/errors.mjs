export const APPLICATION_ERROR_CODES=Object.freeze(['VALIDATION_ERROR','PERMISSION_DENIED','NOT_FOUND','CONFLICT','INSUFFICIENT_STOCK','CREDIT_REQUIRES_CUSTOMER','ACCOUNTING_PERIOD_CLOSED','DUPLICATE_COMMAND','FINANCE_POSTING_PENDING','FINANCE_POSTING_FAILED','RECONCILIATION_INCOMPLETE']);
export class ApplicationError extends Error{constructor(code,message,options={}){super(message,{cause:options.cause});this.name='ApplicationError';this.code=code;this.details=options.details??null}}
export function normalizeApplicationError(error){if(error instanceof ApplicationError)return error;const m=String(error?.message||error);if(/Walk-in|registered Customer|required.*customer/i.test(m))return new ApplicationError('CREDIT_REQUIRES_CUSTOMER','Credit or partial payment requires a registered Customer.',{cause:error});if(/insufficient stock|fabricate inventory|stock pool/i.test(m))return new ApplicationError('INSUFFICIENT_STOCK','Insufficient stock is available at the selected location and ownership pool.',{cause:error});if(/period is closed|period is soft_closed/i.test(m))return new ApplicationError('ACCOUNTING_PERIOD_CLOSED','The accounting date is in a closed accounting period.',{cause:error});if(/permission|authorized/i.test(m))return new ApplicationError('PERMISSION_DENIED','Your role cannot perform this action.',{cause:error});if(/conflict|duplicate|Unique index/i.test(m))return new ApplicationError('CONFLICT','This command conflicts with existing evidence.',{cause:error});if(/missing|does not exist|not found/i.test(m))return new ApplicationError('NOT_FOUND','The requested record was not found.',{cause:error});return new ApplicationError('VALIDATION_ERROR',m,{cause:error})}
export const commandResult=(primaryEntity,options={})=>Object.freeze({ok:true,code:'OK',primaryEntity:primaryEntity??null,generatedDocuments:options.generatedDocuments??[],warnings:options.warnings??[],followUpActions:options.followUpActions??[],postingStatus:options.postingStatus??'not_applicable'});
export const commandFailure=error=>{const x=normalizeApplicationError(error);return Object.freeze({ok:false,code:x.code,message:x.message,details:x.details,primaryEntity:null,generatedDocuments:[],warnings:[],followUpActions:[],postingStatus:x.code==='FINANCE_POSTING_FAILED'?'posting_failed':'not_applicable'})};
